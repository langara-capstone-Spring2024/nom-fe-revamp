import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  label: {
    marginBottom: 6.5,
    color: "#3c3c3c",
  },
  error: {
    marginTop: 8,
    paddingHorizontal: 8,
  },
});

export default styles;
