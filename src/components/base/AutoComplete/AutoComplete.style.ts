import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  label: {
    marginBottom: 8,
  },
  error: {
    marginTop: 8,
    paddingHorizontal: 8,
  },
});

export default styles;
