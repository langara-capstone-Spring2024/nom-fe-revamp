import { View, Text } from "react-native";
import { ButtonSampleProps } from './ButtonSample.props';
import styles from './ButtonSample.style';

const ButtonSample = (props: ButtonSampleProps) => {
  return (
    <View>
      <Text> ButtonSample</Text>
    </View>
  );
};

export default ButtonSample;
