import { StyleSheet } from "react-native";

const styles =
  StyleSheet.create({
    tabBarStyle: {
      padding: 20,
      borderRadius: 20,
      height: 80,
      position: "absolute",
      bottom: 0,
      left: 20,
      right: 20,
      alignItems: "center",
    },
  
  });

export default styles;
