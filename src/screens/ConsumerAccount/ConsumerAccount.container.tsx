import ConsumerAccountView from './ConsumerAccount.view';

const ConsumerAccount = () => {
  const generatedProps = {
    // generated props here
  };
  return <ConsumerAccountView {...generatedProps} />;
};

export default ConsumerAccount;
