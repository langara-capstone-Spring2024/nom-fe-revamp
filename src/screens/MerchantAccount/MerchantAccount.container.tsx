import MerchantAccountView from './MerchantAccount.view';

const MerchantAccount = () => {
  const generatedProps = {
    // generated props here
  };
  return <MerchantAccountView {...generatedProps} />;
};

export default MerchantAccount;
