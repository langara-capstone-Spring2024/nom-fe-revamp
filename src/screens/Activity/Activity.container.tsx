import ActivityView from './Activity.view';

const Activity = () => {
  const generatedProps = {
    // generated props here
  };
  return <ActivityView {...generatedProps} />;
};

export default Activity;
