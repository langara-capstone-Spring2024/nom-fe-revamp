import { StyleSheet, View } from "react-native";
import React from "react";
import Stripe from "../../components/module/Stripe";

const StripeCollection =  () => {
  return (
    <View>
      <Stripe />
    </View>
  );
};

export default StripeCollection;

const styles = StyleSheet.create({});
