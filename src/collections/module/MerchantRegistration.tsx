import { ScrollView, StyleSheet } from "react-native";
import React from "react";
import MerchantRegistration from "../../components/module/MerchantRegistration";

const MerchantRegistrationCollection = () => {
  return (
    <ScrollView>
      <MerchantRegistration />
    </ScrollView>
  );
};

export default MerchantRegistrationCollection;

const styles = StyleSheet.create({});
